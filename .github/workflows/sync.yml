name: sync

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  sync:
    concurrency: sync
    runs-on: ubuntu-latest

    steps:
     - name: clone
       run: |
         git clone \
           --depth 1  \
           --filter=blob:none \
           --sparse \
           https://${{ secrets.ROBOCDNJS_TOKEN }}@github.com/cdnjs/cdnjs.git
     - name: sync
       run: |
         wget https://github.com/cdnjs/tools/releases/download/c0fc0b5bcc31e8064fdfb4e74b30209b866cff992/git-sync
         chmod +x ./git-sync
         cd cdnjs
         git config user.name robocdnjs
         git config user.email cdnjs-github@cloudflare.com
         ../git-sync ./last-sync cdnjs-outgoing-prod
         
     - name: push
       run: |
         cd cdnjs
         git push https://robocdnjs:${{ secrets.ROBOCDNJS_TOKEN }}@github.com/cdnjs/cdnjs.git
